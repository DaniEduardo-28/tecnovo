function init_paginator(a,b,c){
  a=a,
  itemsPorPagina=b,
  numerosPorPagina=c
}

function creaPaginador(a){
  paginador.html(""),
  totalPaginas=Math.ceil(a/itemsPorPagina),
  $('<li class="first_link"><a href="#"><< Primero</a></li>').appendTo(paginador),
  $('<li class="prev_link"><a href="#">< Anterior</a></li>').appendTo(paginador);
  for(var b=0;totalPaginas>b;)
    $('<li class="page_link"><a href="#">'+(b+1)+"</a></li>").appendTo(paginador),
    b++;

  numerosPorPagina>1&&($(".page_link").hide(),
  $(".page_link").slice(0,numerosPorPagina).show()),

  $('<li class="next_link"><a href="#">Siguiente></a></li>').appendTo(paginador),
  $('<li class="last_link"><a href="#">Último>></a></li>').appendTo(paginador),

  0==pagina&&(
    paginador.find(".page_link:first a").addClass("active"),
    paginador.find(".page_link:first").addClass("active")),
    paginador.find(".prev_link").hide(),

  paginador.find(".page_link").click(function(){
    var a=$(this).find("a").html().valueOf()-1;
    return cargaPagina(a),!1
  }),

  paginador.find(".first_link").click(function(){
    var a=0;
    return cargaPagina(a),!1
  }),

  paginador.find(".prev_link").click(function(){
    var a=parseInt(paginador.data("pag"))-1;
    return cargaPagina(a),!1
  }),

  paginador.find(".next_link").click(function(){
    var a=parseInt(paginador.data("pag"))+1;
    return cargaPagina(a),!1
  }),

  paginador.find(".last_link").click(function(){
    var a=totalPaginas-1;
    return cargaPagina(a),!1
  })

}

function get_data(){
  cosole.log("nada")
}

function set_callback(a){!function(b){
  b.get_data=function(){a()}
}(window||{})
}

function cargaPagina(a){
  pagina=a,
  desde=pagina*itemsPorPagina,
  get_data(),

  pagina>=1?paginador.find(".prev_link").show():paginador.find(".prev_link").hide(),

  totalPaginas-1>pagina?paginador.find(".next_link").show():paginador.find(".next_link").hide(),

  paginador.data("pag",pagina),numerosPorPagina>1&&($(".page_link").hide(),

  totalPaginas-numerosPorPagina>pagina?$(".page_link").slice(pagina,numerosPorPagina+pagina).show():totalPaginas>numerosPorPagina?$(".page_link").slice(totalPaginas-numerosPorPagina).show():$(".page_link").slice(0).show()),paginador.children().removeClass("active"),paginador.children().eq(pagina+2).addClass("active");

}

var paginador,totalPaginas=0,itemsPorPagina=4,numerosPorPagina=4,desde=0,pagina=0;
